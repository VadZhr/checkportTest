const PROTOCOL = 'https';

const PUBLIC_API_HOSTNAME = 'testjob.checkport.ru';

// const API_BASE = `${PROTOCOL}://${PUBLIC_API_HOSTNAME}/`;

const API_BASE = 'https://checkport-test-wudd.vercel.app/'

const GET_FILIALS = '/filial/';

export {
    PROTOCOL,
    API_BASE,
    GET_FILIALS,
    PUBLIC_API_HOSTNAME,
};
